# HR03 正式组织岗位上的人员导入：施工与验收

2026-09-06；`penghaibin9/renshi`；唯一施工分支
`fix/production-readiness-20260904`，Draft PR #53。基线 `629b154`；
文件解析独立提交 `a46a5c38d45d01df0b6dc4130a3b7f9d4027e96c`。
本次接入完整导入链仍须取得本提交自身的真实运行结果，不能继承基线绿灯。

## 学校办理路径

学校已有真实 HR02 组织和空缺岗位 → HR03 名册 → 下载 xlsx 模板 →
填写已核验在职人员 → 上传预检 → 下载原文件行号对应的错误清单 →
勾选确认提交有效行 → 刷新名册/任务 → 任职归属及 HR02 岗位占用回读。
新招聘走 HR05，外聘走 HR08。导入不创建登录账号，不伪造 Employee，
不切换学校 Authority 状态，不代替编制方案或考核薪酬制度审批。

## 同一份正式业务数据

复用 ImportService、PersonIdentityService、StaffMasterService、EmploymentService、
AssignmentService，不增加人员/部门/导入任务数据库。组织与岗位通过当前学校的
稳定编码查询，不信任上传文件的租户编号、数据库 ID 或旧部门映射。
同文件/本校工号重复、证件重复、姓名生日疑似重复均进入人工核对，不覆盖原档案。
已有复杂任职或后续版本的迁移保守拒绝，请走正式任职分段办理。

## 文件边界

xlsx 工作表必须名为“人员导入”；同时支持 UTF-8/BOM CSV。
限制：5 MB、5000 数据行、32 MB 解压内容和 200 个压缩成员。
工号、证件号、组织与岗位编码必须按文本填写，不猜测恢复被 Excel 丢失的数字。
拒绝重复/未知表头、表头外数据、公式、Excel 错误、宏、外部引用和嵌入对象。
检查实际 XML 行列坐标及 worksheet 关系，不只依赖可被错误声明的 dimension；
额外行列、重复坐标不得被静默截断。读取前 reset_dimensions，保留原文件行号。

模板为“人员导入 / 填写说明 / 本校组织岗位”三表。参考最多 2000 个岗位，
不是容量承诺；没有对应查看权限时只提供格式，不附本校台账。
错误工作簿只含行号、字段、错误代码和安全说明，不附证件或生日；文字强制文本类型。
新上传证件值在暂存前使用现有加密服务并绑定学校/任务/行号。
历史明文暂存清理、保留期限与密钥轮换不是本次已完成范围。

## 事务、并发与中断

预检原子保存暂存任务、250 行一批的校验结果及预检审计；不写正式人员。
预检只作参考，不锁定容量。提交逐行再次核对版本、有效日期、组织归属、
人数/工作量、未来任职区间和有效 HELD 预占。正式任职创建/切换共用岗位行锁。
每行 Person/Identity/Staff/Relationship/Assignment/事件/审计及提交标记一起成功或回滚。
不同数据行独立处理，不把整文件包装成长期事务。

每次 HTTP 最多 100 行，8 秒软预算在行之间检查，不承诺单个 SQL 不超时。
未完成回 READY_TO_COMMIT，保留原任务与成功行；再次勾选后继续下一段。
终态重复提交只回读；过期执行者由任务令牌和每行锁阻断。沿用 30 分钟失联租约。
网络失败只表示结果未知，要求读取原任务，不能自动重传整批或声称已全部回滚。

## 访问与审计

上传/提交/状态/错误下载检查当前学校及 `hr.staff.import`，并限定 SCHOOL 范围。
新任务由上传者持有，同校其他导入员不可直接读写；旧无上传者标记的任务暂按原权限行为。
只读用户请求写入 403；另一学校访问任务 404。标准 CSRF 校验不绕过。
预检、每行正式提交、完成、错误清单下载留审计；各正式服务收到实际 actor。
异常不将 SQL、密钥、证件值或数据库细节返回浏览器。

## 验收与证据边界

- `test_import_workbook`：25 个无 Django 的真实文件处理测试；本地已通过。
- `test_import_hr02`：原 25 个真实 Django/MySQL 测试，包括两个线程争用同一岗位、
  预检后冻结/占位、审计失败回滚、无权访问、上传者绑定、分段继续、日期边界与加密行绑定。
- 继续执行原 `test_imports`、`test_round4_export_import`、`test_production_closure`、
  `test_v2_roster_contract` 和原学校资料/HR02/提权门，不缩减既有检查。
- 数学规则脚本共 13 个，包含 300 组区间峰值对照；它不执行 ORM，不能代替数据库并发测试。
- 离线 DOM 原 18 场使用真实 Chromium、生产 JS 与进程内受控响应；它不构成业务 E2E。
- 原学校资料 18 条 + HR02 9 条及独立 MySQL 核验通过后，继续同两校、三普通账号，
  在浏览器创建一条真实合成测试人员并拒绝一条错误行；HR03 9 条 E2E 及独立 MySQL 核验
  检查正式归属、重复提交、只读拒绝、跨校拒绝、8 类 actor 审计及暂存加密。

CI 工件记载 productHead 与 git rev-parse HEAD，分别代表产品 SHA 和实际 PR 合并测试快照。
本文件不签署 release-ready。没有新代码运行证据时，PR 继续 Draft；不合并、不部署。

## 2026-09-06：正式接口测试和后端恢复状态

已读取 `5e275e90f3373e728b3c948f12e6adbba7b19a51` 的运行 `33984899948`：
197 项集中测试中 193 项通过、2 项失败、2 项报错。四项均是测试请求旧
`/api/hr/v1/staff` 地址得到 308，而不是正式接口 `/api/v1/hr/staff` 的 JSON。
旧地址只重定向是现有路由设计，不能删去兼容层或把 308 当作人员提交成功。

本次可独立验收的后端变更：

- 状态接口增加 `canResume`：仅 COMMITTING 且原执行者租约过期才为真。
  不返回执行令牌，不修改任务；正式提交仍按原上传者、权限及数据库行锁重新校验。
- 原 API 测试改为请求现有正式地址；新增旧路径保持 308 且无写入副作用的回归。
- 新增活动执行者不可接管、失联执行者过期恢复、最终审计失败后只完成原任务
  而不重复创建人员三项回归。导入数据库测试合计 29 项，必须以本次 SHA 运行结果为准。

截至 fdc9593 的未提交部分（历史记录，后由下方 7cc546f 接入）：名册模板的
正式地址直连、恢复按钮及“已提交但名册刷新失败”提示修复当时仍在本地；
实际导入浏览器脚本的正式响应监听当时也未写入。不能把这些行为算入旧 SHA。
独立后端变更不依赖页面修改，既有前端能忽略新增状态字段。

当时本地已复核 25 项文件解析、13 项业务决策和完整补丁的 24 场离线浏览器检查；
这些不是旧远端版本的整站通过证明。锁定依赖本地安装未成功，Django/MySQL
验证交由同一分支工作流，且不跳过已知人员浏览器阻断。

## 已读取的 fdc9593 实际运行结果

产品提交：`fdc9593cb6bb7f7dc6ecf38a095f3fa003676c81`。
实际测试快照：`5543141d9565a01fad5eb7f115345a76a4b8bfb5`。
运行：`33995560324`；实际产物：`9978182396`。
下载工件 SHA-256 与 GitHub digest 一致：
`a8828caffdb875bbbaca3ddb4cb61342831c0a0761c692722e4bc25abecca793`。

- 集中 Django/MySQL 测试 201 项全部通过，包含 29 项导入与恢复测试。
- 学校资料 18 条、组织岗位 9 条真实 Chromium 断言通过，两阶段 MySQL seal 为 PASS。
- 人员导入浏览器只完成模板下载一项，仍在读取旧地址 308 响应正文时失败；
  没有取得该阶段最终人员 MySQL seal。整个工作流不能据数据库测试通过签为成功。

## 名册读取修复及 ea59770 验证结果

在既有 `StaffListSelector` 内补齐两处取数约束，不建立第二套名册：

1. 组织显示名称复用 HR02 的 `FORMAL_STATUSES`，避免更高编号的 DRAFT、
   REJECTED、CANCELLED 版本覆盖正式名称；保留同校、as-of 和半开日期边界。
2. 批量任职查询同时加载随后必读的聘用关系，不再每个教职工单独补查该关系。

`test_staff_list_readback` 五项数据库回归已接入集中测试：非正式名称排除、
日期切换、跨校拒绝、无正式名称时编码回退、1 人与 50 人名册查询次数一致且不超过 12 次。

已读取 `ea5977072322405033cd5d3366313bb44f220269` 的运行 `33996562542`，
实际快照 `bd4f1dfa4027399750df6b3c10ac7d838a756f1a`，产物 `9978467902`。
工件摘要已验证为 `f6b62e429669e45d30dd3230619ed8e4f79217a85f35c2dbe328bbb55f7d4046`。
206 项集中测试全部通过，Ruff 通过；学校资料 18 条和组织岗位 9 条及各自数据库封存通过。
人员浏览器仍只完成一项、未产生人员最终封存，所以该版本的整条工作流未通过。

## 7cc546f：页面与浏览器正式接口收口

代码提交 `7cc546fd3ebcfc68f1babca0e178c0385efa865d` 已把原名册、导入上传、
提交、状态、模板、错误清单与导出请求直连现有 `/api/v1/hr/staff` 正式入口。
旧地址的 308 兼容层、CSRF、学校权限、上传者约束和服务事务均未修改。
两份真实浏览器脚本和页面合同同步更新，不把旧地址重定向当作写入成功。

前端只在服务器 `canResume === true` 时提供恢复原任务的确认操作；
活动执行者仍不可接管。已确认提交与随后名册刷新失败分开提示；
提交响应本身丢失则继续显示结果未知，必须回读原任务，不自动重传或重复写入。

本轮重新运行：25 项文件测试、13 项业务决策测试、24 场离线 Chromium 交互全部通过。
本机 HTTP 导航被浏览器管理策略拒绝（ERR_BLOCKED_BY_ADMINISTRATOR），没有更改策略绕过。
离线 DOM 检查不冒充真实 HTTP 或 MySQL；真实三角色业务链仍由原 Actions 工作流执行。

技术依据：Playwright 把重定向后请求视为独立 Request；监听必须对应实际的正式接口响应。
官方说明：https://playwright.dev/python/docs/api/class-request#request-redirected-from

## 7cc546f 的完整运行回执：人员浏览器通过，最终审计核验未通过

已读取运行 `33997680373`，实际测试提交 `8df1dd1d531c7b0a147870983aa1e5b9399beb30`。
产物 `9978849427` 的实际文件摘要与 GitHub 元数据一致：
`199a538d4cefc144827345416cef8900fb98d7871f231272d951a56b6d123fcc`。

206 项集中 Django/MySQL 测试全部通过。学校资料 18 条、组织岗位 9 条及两段数据库封存通过。
人员导入 9 条真实 Chromium 断言全部通过，错误诊断为 null；预检只暂存的独立核验也通过。
但人员最终 MySQL 核验在 `Audit actor/actions mismatch` 处失败，不能把整个工作流标成通过。
同提交的独立 Quality / MySQL Contract 工作流 `33997680441` 成功，不代替此处失败的封存。

服务器日志显示模板下载成功 GET 两次，错误清单成功 GET 两次；浏览器 trace 各记录一个 fetch
及一个 blob 下载事件。额外请求与审计数量不符之间存在明确的待核对关联，但没有取得该次
最终数据库审计明细，不能武断归因为审计缺失、演员错误或某个浏览器缺陷。
生产错误清单入口仍对每次实际读取记审计，不为消红去重、删除或放宽审计。

## 同轮名册回读和下载请求归因

在既有页面补充最新请求顺序保护；新查询立即清理旧结果、导出选择、日期和分页；
非 JSON、无效结构和网络失败显示失败，不假装零人。旧失败不能清除新结果，旧成功也不能
掩盖新失败。已确认的人员提交仍与后续名册查询结果分开，不重复创建已保存人员。

`scripts/school_staff_import_readback_contract.py` 九个场景分别在桌面和手机宽度执行。
相同测试旧代码 18 场仅 2 场通过，修复后 18 场全部通过；原 24 场导入离线交互也通过。
这是生产脚本加进程内响应的组件检查，不是 HTTP/MySQL；已接入原工作流，没有删除原门禁。

错误清单下载在审计及响应 `X-HR-Request-ID` 中关联服务器生成的请求标识。重复读取仍分别
留痕。新增两个数据库测试检查两次读取具有独立审计、审计失败时不得返回无留痕工作簿；
它们由完整 Quality 的 hr_staff 测试发现，新提交的结果仍待核实。

额外尝试的下载界面优化和审计明细脚本写入被工具拦截，未进入本次分支版本，不算已完成。
最终人员核验继续保留原严格条件；本轮不宣称该审计差异已经修复。保持 Draft。

### 学校办理人须知

先确认本校组织岗位与容量。不要修改模板表头或“人员导入”表名；工号、证件、组织岗位
编码按文本填写。模板中的岗位参考不等于容量预留，正式提交仍会检查；不能把全校人员
塞入初始化产生的一个空缺岗位。预检不创建正式人员，确认后只提交有效行；修正失败行
时不要重新上传已成功人员。中断后回到原任务读状态，只有服务器允许时才明确恢复。
这条导入不创建登录账号；人员任职、账号邀请和角色授权需要各自的后续验收。
