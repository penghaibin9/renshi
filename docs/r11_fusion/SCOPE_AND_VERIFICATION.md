# V14 范围与验证

本轮只做HR09资质来源元数据 → HR12审定依据绑定/归档/读取/导出/人工单样本核对；完整HR01–HR18原系统仍保留。101条是来源与选择清单，不是本版完成101条验收。完整映射见随交付的Mapping.xlsx和clause-mapping.json。

## 新增核查

63项方法执行：61通过、2项MySQL专用跳过。涉及原结果计分、来源校验、身份/证书/规则后续变化、历史更正链、旧档案、非法学校、无权限、审计失败、文件损坏/路径、Excel公式注入、样本缺失和有限数值。

原HR12全集合在本次隔离设置下339项：330通过、0断言失败、4环境导入错误、5跳过。未改V13对照276项：269通过、同4环境错误、3跳过。

原HR09集合本版与未改V13对照均为128项：120通过、同2断言失败、5导入错误、1跳过。两项旧失败分别为HR10来源映射缺口期望与HR09缺学校响应；未因本轮修改新增，但仍未关闭。导入错误主要依赖auditlog/完整学校应用，不算通过。

## 浏览器

25项实际页面/交互/文件下载检查通过，page error 0。用原HR12模板/脚本和CSS渲染，显式传输到真实Django视图与SQLite。HR09资质/附件元数据为实际ORM；HR03人员provider信封、全局壳层、认证/成员权限与CSRF传输是明确夹具。读取503为故意故障注入，正式结果与归档/访问审计经实际API落表。没有原生TLS/Cookie登录、学校SSO或MySQL证明。

视口1440×1000、1093×614、768×1024、390×844；仅是尺寸检查，不是Windows实机或浏览器125%验收。测试截图、姓名、85分/86分均为合成样例，绝不当校方签认。

## 迁移

在SQLite聚焦设置实际执行HR12迁移链到0028，其他依赖应用syncdb；新表、权限状态、空功能回退重放、已有证据禁止回退、模型漂移共8项核查通过。MySQL专用触发器分支未运行，全部人事生产迁移未执行。

## 原有阻断及未移植

不复制非学历培训收入/积分、湘培接口、外部认证与安装方式，不替换现有工资制度。完整资质文件版本库、全域统计同源、全域权限、同步重试、3/5秒与3000在线/100并发、真实学校人工样本100%、运维承诺均没有自动完成。

原完整horilla.settings检查在environ导入处失败，宿主没有Docker/MySQL。本版不是部署或采购验收批准；release_approved=false。
