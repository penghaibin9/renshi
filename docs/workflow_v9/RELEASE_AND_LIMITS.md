# V9 发布契约、验证范围与剩余缺口

## 连续基线

V8包：Yueke_University_HR_R2_Payroll_Depth_V8_20260919.zip。
SHA256：c686f7c20e383096c88b4d7a64377b55582a3c1927cf12ed63238f655047e6e1。

V9复用原任务、状态机、四层人员、生效事实、审计和持久幂等结构；没有平行新建工作流引擎、工资系统或员工总库。新增的是可执行的服务与办理入口，而不只是样式。

## API兼容性必须处理

新增原HR05命名空间下的责任任务/责任账号查询、结果核对、显式生成任务/材料、材料单项读取、任务分派和协同结案接口。保留原canonical路由及308兼容映射。

原开始/完成/豁免、材料上传/核验等写接口强化版本和Idempotency-Key；录用通知审批/签发需要If-Match内容指纹。不能说请求契约全部没变。第三方调用者、旧页面缓存、移动端若调用这些接口，需同步接入前置条件；未接入时应被拒绝而不是默默绕过。

前后端一起发布并更新静态资源缓存。没有新增数据库迁移；但已有V8迁移必须保持，不能拿“V9无迁移”跳过V8升级。

原责任账号映射和正式人员/任职缺口不得由脚本猜测修补；先核对授权、本校成员及正确ID。自动任务仍等待原提供方；不得为了验收人工造成功。

## 本轮实际验证

- HR05：268测试通过；真实SQLite ORM与既有单元/契约测试混合。
- HR04：231测试执行，其中212通过、19项原有MySQL/并发专用场景跳过。
- HR15：215测试通过，薪酬目录相对V8字节未改。
- HR01：13项原个人待办定向测试通过；另3项新个人待办ORM检查计入HR05总数，不重复相加。完整HR01设置因缺auditlog依赖未能收集，未声称全HR01通过。
- 跨域HR02预占→既有HR04交接→HR05材料/激活→HR03四层记录→协同结案测试3项通过，其中新1项已计入HR04总数。测试从受控的已接受录用边界开始，不是从零线上招聘到最终任职的全链SSO验收。
- 浏览器76项检查通过，页面脚本异常0。原模板/业务脚本经set_content渲染，显式Python HTTP桥接到实际Django视图/服务及SQLite。真实接口执行分派、开始、完成、材料上传/核验、结案、审批/签发及CSRF拒绝；身份成员权限来源、起始数据和外壳为夹具。

浏览器地址导航实际被管理策略禁止；未绕过政策。读503和“服务端成功但前端回执丢失”为明示故障注入，写操作已实际落库后再测试原编号重放。不是写死成功JSON，也不是生产Cookie/TLS/SSO联调。

桌面1440×1000、1093×614@1.25等效125%、768×1024、390×844检查3类页面及责任人弹窗。125%是可用空间/密度近似，不是Windows系统实机缩放。没有做现场人事老师计时，不编造效率提升百分比。

## 不可宣称已完成

MySQL迁移/锁并发、真实SSO/完整角色院系权限、完整HR01–18生产启动和回归、银行/税局/教务科研外部交付、Windows实机、万人规模压测尚未完成。MySQL环境未能建立，不能把SQLite结果当成MySQL证明。

本轮HR05针对同校成员和具体任务责任做访问检查，但未完成全系统院系级工资/档案权限穿透。外部开通/通知事件送达、全量材料历史版本库、所有试用状态结案、业务超时升级/SLA和全域审批授权也未全覆盖。

V8剩余的校院两级绩效预算、年度预发清算、负向追扣、复杂社保反算、专项税务及真实学校标准/实账仍在其原限定范围，本轮没有借任务办理改造标为完成。工资源码已保留并重跑回归。

## 回滚

先停新写入再核对已产生的任务、材料、结案、录用事实及待发事件。V8不包含全部V9命令保护，不能把回滚代码当作业务回滚；不删除新回执、不恢复旧的越权接口。优先前向修复。确需恢复时使用经过业务核对的备份方案，且不覆盖其他并行变更。
