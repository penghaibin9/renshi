# 学校开户前基础配置｜本轮施工状态

日期：2026-09-05。仓库：`penghaibin9/renshi`。只在 PR #53 的
`fix/production-readiness-20260904` 施工，不合并、不改学生仓库。
本轮续施工父提交：`f3be6fb1e76c87092c2aeb76f61ba8dd2eb41d10`。
该父提交实际测试快照：`7843ad6a3ccede964044ebe2d6e77489ad1e1ebe`；后续修复必须重跑。
本文件不覆盖 CURRENT_STATE / CORE_BUSINESS_FLOW_ACCEPTANCE 的发布门禁。

## 已写入的代码

- 平台操作员判定不再等同于 `is_superuser`：有 Employee 或学校角色授权的账号属于学校身份；停用账号被拒绝；身份读取异常不得授予平台权限。
- 学校创建和全局公告有效期修改改用平台操作员资格；学校资料的租户检查也覆盖历史学校超级管理员；平台进入学校仍须有效限时提权。
- 学校管理员可在尚无 Employee 档案、尚无学校图标时建立本校会话；不制造虚构教职工。
- 现有系统设置中新增“学校管理中心 / 首次配置与学校资料”，保留既有齿轮入口与旧链接；有 Employee 的账号保留偏好设置首屏，无 Employee 的首管先进入学校管理中心。
- 新页面 `/settings/school-management/` 实际保存学校资料，字段白名单、CSRF、学校/操作者绑定的限时快照凭证、行锁与审计同事务；并发过期凭证返回 409；非法表单返回 400；审核日志失败回滚。
- 状态接口 `/settings/school-management/status/` 读取本校 HR02 组织/岗位、HR03 主档和普通学校管理账号；无权限不展示数据，不可用不当作空数据；不可用返回 503。
- `productionReady` 始终为 false：已有记录不代表正式生效，更不代表学校业务验收完成。
- 设置子视图去掉重复实现并修正平台操作按钮判定；保留唯一的 request-safe 实现。
- 原设置壳以原 blob 原样保存在 `settings_shell.html`，`settings.html` 继承它，只补折叠区显隐和点击几何规则，保留原搜索与 HTMX 行为。

## 2026-09-05 续施工：真实登录与学校边界

旧提交 `32ba6e8` 的运行 `33952961315`：43 项 MySQL 合同测试通过，
真实 Chromium 在首个账号登录后仍落到 `/login/`，不能算浏览器验收通过。
本轮据此修改账户入口，而不是制造 Employee 档案或减弱浏览器断言。

- 新 `base.account_urls / account_views` 显式注册在旧 base 路由前，沿用原 URL 和反向名称；登录、默认首页、页头通知读取不再把 Employee 当作必需的账户凭证。
- 仍走 Django 密码认证、会话、CSRF 和既有 remember-me 策略。停用用户、归档 Employee、无学校授权且非平台身份的无档案账号拒绝登录。认证时清除旧学校选择与平台提权；实际业务权限仍由各入口检查。
- `SafeCompanyMiddleware` 在旧 clamp 逻辑之前拒绝外校、失效和非法 ID，不把 POST 自动改投另一所学校；联合/未选择范围不自动取首校，历史学校超级管理员也不绕过。
- `base/dashboard.py` 删除旧八项初始化存在性推断，调用学校中心同一 `setup_summary`；无确定学校不读取初始化；收起提示也必须绑定当前有权学校，禁止写入 null/外校偏好。原统计函数保持不变。
- 新增 `base.test_account_bootstrap` 的 17 个测试方法，覆盖真实密码登录、完整模板/中间件、默认入口、通知、CSRF、撤权、非法学校、联合范围与初始化状态同源。
- 保留浏览器原 13 条断言，新增通知读取、默认首页和只读角色保存后回读，合计 18 条；MySQL 封存仍验证没有伪造人事/组织事实且本校审计只有真实修改。

本轮本地完成 8 个 Python 文件语法编译、12 项隔离策略/源码一致性检查。
本地没有 Django/MySQL，因此 17 个新增 Django 测试、18 条浏览器断言和
MySQL 封存必须以新提交的 Actions 输出为准；代码写入不等于生产验收。
工作流仍只有 `contents: read`，不自动提交、不降低既有门禁。

旧 `base.views` 中账户函数仍保留在遗留源文件，但正常根 URL 解析已选择新账户入口；
本轮不宣称遗留代码已全部清理。路由归属由新增测试核验。

## 同轮补充：既有系统设置浏览器验收自检

旧提交运行 `33952961343` 的诊断为 `KeyError('school_a_updated_name')`，
仅完成 6 条浏览器断言；不能归因于学校资料保存已经失败或全部流程通过。
补齐原工作流 seed 的学校更新名称、更新地址和 B 校原地址，保留原真实编辑步骤。
数据库封存新增 A 校资料已更新、B 校资料未变化的独立断言，补入漏记的普通教师
学校列表拒绝项，并拒绝重复证据。浏览器矩阵为 21 条，输出 `mysql-seal.json`
分别记录产品提交与被测试的 PR 合并快照。

旧门禁测试引用已不存在的浏览器函数，现按实际登录、点击、回读、独立角色、
种子字段完整性和数据库封存重新校验，保留原四类验收意图并扩展为 6 项。
相同 6 项结构检查在旧脚本/工作流上 4 失败，在修复后 6 通过；
这是离线自检，不等于 MySQL 或 Chromium 全链通过。

## 155fbfd 真实运行后的公共入口修复

提交 `155fbfd6c9155b273769d4aff9a7ef3fe5727b5f` 的运行 `33958385784`
已完成真实 MySQL 迁移、Ruff 检查和 60 项 Django 测试：53 项通过、7 项报错。
七项报错均发生在学校账号渲染页面时，调用链为
`horilla.config.get_MENUS → sidebar → project.sidebar.menu_accessibilty → any_project_manager`，
旧侧栏直接读取不存在的 `user.employee_get`。真实浏览器登录 POST 已返回 302，
但学校管理页模板因同一问题返回 500；浏览器断言 0 项完成，MySQL 封存跳过。
这不构成初始化办理通过。

后续补丁在公共 `horilla.config.sidebar` 入口区分账号与人员：无 Employee 的账号
不调用这些遗留人员侧栏提供者，清空本请求遗留菜单；不伪造档案，不吞掉其他异常。
学校设置与正式 HR 菜单保留独立权限检查，已有 Employee 的原菜单授权路径不变。
新增 4 项侧栏边界回归；本轮账户测试由 17 项扩为 21 项，必须在新 SHA 重跑。

系统设置浏览器还根据旧 trace 中真实 DOM 修正编辑链接的查询字符串匹配，
通过真实国家/省份联动选项完成必填表单，并等待真实 HX-Redirect 导航后再回读。
没有强制点击、关闭原生校验、移除编辑或放宽数据库结果检查。
当前这些后续修复仅有本地隔离与结构检查；新 SHA 的数据库、浏览器和封存结果
仍须单独读取，不能沿用 155fbfd 的部分绿灯。

## 2026-09-05 本轮：账号资源、表单隔离与安装入口边界

本轮沿用 PR #53。父基线 `cca0147e63abb4fd7acf9de0a34b50ee2fcead34`，
其被测试合并快照 `3cce868a73eb9941d6ee397482cbe3f233106927` 与产品基线树一致。

### 已核验的旧基线结果

- 系统设置多角色运行 `33959686761` 已成功：三名普通角色、真实 Chromium、
  21 条证据及独立 MySQL 核验全部通过。不能扩大解释为“所有设置入口通过”；
  同基线的设置全量入口巡检仍失败。
- 空学校运行 `33959686745` 为失败：64 项 Django 测试中 62 通过、2 个夹具错误；
  两个测试错误地用仅允许具体学校的 `tenant_context("all")` 表示网页联合范围。
- 空学校浏览器已完成管理员 A 的 9 条断言，包括资料保存回读、过期页面 409、
  手机宽度实际保存；随后因页面错误中止，其他角色和 MySQL 封存尚未运行。
  错误来自旧 `country.js` 把新文本字段当下拉框，以及旧安装应用资源接口
  对合法无 Employee 账号返回 HTML，导致前端 `installed_apps.includes` 报错。

### 修复提交 `06723597df26bd9cbc516c584444ccadd8958c7e`

- SchoolProfileForm 使用独立 `school-profile-*` 字段 ID；保留原字段名称、
  POST 协议、label 关联及原生校验，不改旧国家下拉框脚本。
- 同路径 `get-horilla-installed-apps/` 由账号入口返回 JSON 资源清单；保留有效
  账号准入和只读方法，不授予人员模块权限，不返回登录 HTML 冒充资源结果。
- 网页联合范围测试使用可恢复的 ContextVar 设置；生产 `tenant_context` 继续
  拒绝联合范围，新增断言确认这个拒绝未被放宽。
- 账户测试由 21 项增加至 26 项，集中合同预计 69 项；既有空学校浏览器 18 条
  和系统设置 21 条均未删除。14 项隔离函数/真实 Chromium 组件检查本地通过，
  不作为完整 Django/MySQL 或网站端到端通过的证据。
- 精确提交运行 `33961727944` 已成功，实际测试快照为
  `45c208a9c09d09be6a6b17d2e5104f43b2b80527`：Ruff 通过，69 项 Django
  测试全部通过；三名普通账号的 18 条真实 Chromium 断言全完成且页面错误为零。
  独立 `mysql-seal.json` 为 PASS：2 所学校，0 伪造人员档案，1 条实际资料修改审计。
  A 校资料保存/回读、另一只读角色回读及写入 403、B 校隔离和跨校写入 404、
  学校账号创建平台学校被 403 拒绝、过期页 409、移动宽度实际保存均已覆盖。
  这只证明已激活账号的学校资料办理闭环，不是整校开户或所有业务可投产。

### 本轮继续封闭旧安装写入口

旧 `base.views.initialize_database_user` 等子处理器只有 HX 请求标记检查，
无法用父页面上的初始化/DEBUG 判断保护直接访问。它们可创建或删除账号、
建立学校及更改旧组织，不应成为第二套学校开通机制。

- 精确保留 10 个旧 URL/反向名称，由 `base.legacy_initialization` 统一返回
  `410 / LEGACY_INITIALIZATION_RETIRED`；不读取表单口令、不调用写入器，
  DEBUG 和平台身份均不能重开。没有创建账号、学校或真实人事业务数据。
- 正常 CSRF 中间件保留；缺少 CSRF 的 POST 返回 403，而不是绕过安全校验。
- 登录页面不再向空库展示旧安装向导。初始平台凭证通过受控部署管理命令建立；
  后续学校开户仍需正式平台服务与首管邀请，不用这批旧网页替代。
- 新增 8 项回归：精确 URL 归属、无数据库方法响应、匿名空库、学校普通账号、
  历史学校超级管理员、平台操作员、DEBUG 不重开及 CSRF。
  四类身份对 10 路由执行 GET/POST，共 80 次请求，并逐次比较 User、Company、
  Department、JobPosition、Employee 全表事实无变化。这些 MySQL 断言须以新
  提交实际运行结果为准，本地只执行 14 项隔离策略/路由与工作流语法检查。

本次封闭的是这 10 个已核实的安装入口，不表示全部遗留写入口已审完。
首管邀请、改密、MFA、组织岗位生效、教职工导入和角色审批完整业务仍待验收。
学生仓库和跨产品开户编排未修改，发布签字仍 PENDING。

旧安装入口封闭提交 `f3be6fb1e76c87092c2aeb76f61ba8dd2eb41d10` 的运行
`33962599361` 已成功，实际测试快照 `7843ad6a3ccede964044ebe2d6e77489ad1e1ebe`：
77 项 Django 测试全部通过，其中包含上述 4 类身份的 80 次旧入口 GET/POST
与数据库不变性校验；18 条真实浏览器断言和独立 MySQL 封存再次全部通过。
不把本次封闭结果当作尚未实现的首管邀请或自动开通服务。

### 同轮全量回归发现：拒绝规则正确，接口错误格式不完整

`0672359` 的 Quality 运行 `33961727960` 执行了 3260 项注册测试，3259 项
通过，唯一错误是 `hr_time.tests.test_s1...test_invalid_company_id_fail_closed`。
新学校中间件在业务函数之前正确返回 403，但只有 `detail`，导致业务客户端
无法读取既定 `error.code = TENANT_CONTEXT_REQUIRED`。

补齐共用拒绝响应的错误码、错误消息、版本、请求标识、生成时间和 no-store；
保留 detail 兼容旧页面，仍在业务调用之前拒绝，不放行外校，不自动换学校。
新增 `base.test_tenant_selection_envelope` 的 2 项中间件响应/ContextVar 回归，
本地 10 项实际函数隔离检查通过；在学校初始化门直接纳入原 HR11 失败
用例；不修改、更不删除原 HR11 测试。这是公共边界修复，不扩展考勤业务。
当前新增结果仍须绑定后续提交实际 CI，不把前一提交的绿灯移用。

## 前一轮离线证据边界

本地执行：Python 语法编译；24 项隔离函数逻辑检查；系统 Chromium 在
1440 / 1024 / 390 三种屏宽共 9 次普通鼠标点击的隔离 CSS 场景。
这些检查没有连接 Django/MySQL，也不是生产站点截图或全流程验收。

新增 `backend/base/test_school_management.py` 的 18 个 Django 测试方法，
包含身份边界、租户拒绝、首次管理会话、并发快照、无权写入、失败回滚、
真实资料保存与审计。它们随已有 Quality 的 `manage.py test base ...`
自动发现；本轮本地环境不具备 Django/MySQL，不能标为已通过。

已有环境完成迁移后，应在隔离 MySQL 测试库执行：

```sh
python manage.py test base.test_school_management base.test_account_bootstrap base.test_legacy_initialization_boundary base.test_tenant_selection_envelope hr_time.tests.test_s1.HrTimeS1TenantFailClosedTests.test_invalid_company_id_fail_closed base.test_settings_runtime_contract base.test_settings_center_contract platform_access --keepdb --noinput --verbosity 2
```

本命令必须遵循既有 CI 测试库创建/迁移约定；不要指向生产业务库。

## 未完成，禁止被本轮提交掩盖

1. 工作台初始化已改为学校中心同源状态，但新 SHA 的完整运行与界面回归尚需收口，不能凭源码或旧 SHA 绿灯签字。
2. 所有遗留全局设置、通用删除/批改、学校表单的完整权限入口清单尚未全面收口。本轮不等于全系统授权审计完成。
3. 完整首管邀请、角色模板配置、审批负责人和制度发布向导尚未交付；本轮是第一段可核验资料办理与配置检查，不是完整自动开户。
4. HR02/HR03 深链仍使用原业务页面。未重新证明空学校从建组织、建岗、导入人员到正式生效的浏览器全链。
5. 本轮覆盖已激活、无需首次改密的学校账号。完整首管邀请、首次改密、MFA，以及无 Employee 的多学校切换交互尚未验收；联合范围目前拒绝隐式选校，不代表切换向导已经完成。
6. 当前 exact-head 的 MySQL、浏览器、Docker、备份恢复、漏洞门、全部必需 CI 必须重新核验；不继承旧 SHA 绿灯。
7. 学生产品的开户编排、跨产品学校映射、身份同步/SSO 均未改动。

## 下一段唯一工作

沿用同一分支，先执行上述数据库用例与既有系统设置多角色 Chromium 门，
修最上游真实失败；核验已替换的首次配置检查，补齐“普通学校管理员从空学校
完成资料 → 组织/岗位 → 人员 → 角色/审批”的真实办理证据。
在此之前保持 PR Draft，不宣称可生产开户，不扩展 HR12–HR18。
