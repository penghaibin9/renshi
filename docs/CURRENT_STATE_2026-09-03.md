# 高校人事系统当前状态（2026-09-03）

## 结论

HR01～HR18 的本地代码与 MySQL 验收门已全部通过，可继续进入真实数据、外部系统和生产运维演练；当前不能把“本地验收通过”表述为“生产上线完成”。

## 本轮可复核证据

- MySQL 8 从空库执行全部迁移成功，`migrate --check` 通过。
- `makemigrations --check --dry-run`：无模型漂移。
- HR01～HR18 联合测试：3200 项通过，0 失败，0 跳过，用时 240.263 秒。
- `hr_authority_gate --all-tenants --require-reconciliation --require-zero-legacy-writes --fail-on-partial`：状态 `COMPLETE`，1 个隔离验收租户漂移 0，旧正式写入尝试 0，告警 0。
- `check --deploy --fail-level WARNING`：使用完整生产安全配置检查时 0 告警。
- Python 语法门与 CI 选定的高风险 Ruff 规则：全部通过。

## 本轮已收口问题

1. 补齐 `project.TimeSheet` 模型元数据迁移，消除迁移漂移。
2. 修复 HR12 数据迁移在 MySQL 上对聚合查询调用无序 `first()` 导致的空库迁移失败。
3. 修复 HR09/HR12/HR13 按日期读取证据时依赖 MySQL 时区表的问题，改为本地日界的时间戳半开区间。
4. 修复 HR01 在权威事实缺失时把本年离退误报为 0 的问题，正确返回不可用状态。
5. 对齐 HR04 的租户成员资格和候选人签名访问票据安全测试。
6. 修复 HR07 文档票据测试关闭响应时破坏 MySQL 测试事务的问题，并给文档绑定补上原子事务边界。
7. 将 HR07 私有文档存储键上限收紧至 255，迁移前显式拒绝超长历史值，清除 MySQL 唯一字符串字段告警。
8. 补齐 HR06 返岗目标岗位失效的确定性测试，并修复异常状态在抛错时被事务回滚的问题。

## 尚需真实环境完成

- HR05/HR07/HR08/HR10/HR15/HR16/HR18 的外部账号、电子签章、培训、银行财税、IAM、资产、档案、报送与交换平台联调。
- 使用学校脱敏副本完成大数据量迁移、组织树、考勤、薪酬和报送性能压测。
- 完成浏览器全流程、可访问性、移动端体验、备份恢复、灾备切换和回滚演练。
- 对 HR10 0015～0019 历史状态收敛迁移做 squash migration 专项；当前五条迁移在空库初始化中合计约 169 秒，不影响正确性，但影响 CI 和灾备恢复效率。

上述事项需要真实凭据、真实数据或生产级基础设施，不能用本地桩数据冒充完成。
