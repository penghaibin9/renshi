# 高校人事系统：第二轮源码接手入口

这是在上一轮修复源码上继续修改后的完整工程，不是补丁说明包。
当前状态：**生产化候选源码，生产验收未放行。**

先阅读 `docs/reports/HR_ROUND2_PRODUCTION_READINESS_2026-09-14.md`。
已执行的检查证据在 `docs/reports/round2_evidence/`。
新增独立规则测试：`python -m unittest discover -s tests/round2 -p test_pure_contracts.py -v`。
新增MySQL运行验收入口：`python scripts/accept_hr_round2.py`（默认预检，不迁移）。

## 交给本地Codex的一条指令

继续接手本包高校人事系统第二轮源码。先读本文件和整改报告，核对原包SHA256、当前本地工程、未提交修改与数据库配置；仅在独立副本施工，若本地已有更新先做差异合并，绝不覆盖原工程。不连接GitHub，不混入学生SaaS或教培项目，不修改生产服务器或正式数据库。按项目requirements.lock和现有部署方式准备MySQL8.4隔离测试库与依赖，禁止SQLite代替。先运行scripts/accept_hr_round2.py预检；确认配置为独立qa_/test_/acceptance_库后，用--run和--confirm-disposable-database传入实际测试库名，执行迁移、新增26项及HR01—HR18完整回归，发现代码问题直接修复并复跑，不得删测试、关闭权限/CSRF/触发器/材料校验或伪造外部成功。再用两所测试学校和四种真实角色跑退休申请、补件、更正、独立审批、生效、数据中心对账，以及越权、重复提交、文件故障、备份恢复与真实浏览器截图。测试账户按现有授权体系分配最小权限，不调整正式账户。对已进入交接/生效阶段的退休改期，先补受控变更链，不能强改日期。输出实际通过、失败、未执行及剩余阻断；全部生产门禁完成前禁止宣布已生产可用。需要动生产数据、服务器、全局安全配置或权限时先向我确认。

## 保持的边界

HR16拥有退休申请与退休事实；HR03拥有档案更正和材料；HR17只根据本人身份调用，不复制第二套审批事实；HR18只统计与保存证据，不回写别的模块。原18模块、Excel、部署脚本与许可文件保留。

八份新增迁移由Django依赖排序，不手工乱改编号。新权限hr.self.apply、hr.exit.flex.review、hr.data.snapshot.capture不会自动授予普通用户，先给测试角色按职责授权。未经学校核定的特殊政策、未接通的外部系统和缺失的真实材料必须阻断，不伪造成功。

严禁直接运行清空演示数据、reset、flush或销毁正式数据的命令。
