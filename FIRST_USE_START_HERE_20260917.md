# 跃科高校人事：最小开户与首次启用候选包

**交付状态：源码候选，不是生产验收通过包。** 这是原完整工程的延续，不是重做的小系统，也不是仅说明文件。仅在云端副本施工；没有连接或修改用户电脑、GitHub、服务器、生产数据库。

## 1. 本轮实际改变

### 开户少填，但不编造信息

首次开户只有学校名称、管理员姓名、邮箱、密码四项。邮箱默认同时作为登录账号；兼容原 `--username` 等参数。地址、国家、省份、城市、邮编、管理员电话可以以后补填，数据库和表单同步放宽，不用“未知地址”或虚构电话占位。新管理员的性别、婚姻状态不自动编造。原有学校资料与旧人员数据不批量改写。

完成正式环境配置与数据库迁移后，部署管理员可运行 `python manage.py open_school`，按中文提示开户；密码隐藏输入并复核。自动化部署仍调用 `bootstrap_production_admin`，密码只通过临时环境变量提供，不作为命令行参数。没有新增匿名公网注册入口，也没有降低密码、租户或部署安全检查。

开户增加安装级互斥记录、原子事务和关联回读。成功只代表学校与首位管理员建立，不代表合同、工资等业务验收通过。相同请求重复执行不重复开户，后补资料不会被空值覆盖；已有其他学校、人员或冲突账号时拒绝自动接管。

### 首页显示实际“下一步”，不靠人工勾绿

学校开户 → 学院/部门/岗位 → 第一位 HR03 正式教职工 → 日常管理角色。进度读取本校后台记录；无权限、无法确认范围时不猜测。开户创建的兼容 Employee 记录不算已经导入第一位真实教师。

旧的六步勾选保留为实施备忘，并明确降格为备忘，不再作为正式启用状态。基础资料齐全仍明确提示需要业务验收；“可以后补”的学校资料折叠显示。

### 教职工导入不再要求用户找数据库编号

新增 `.xlsx` 模板和受控下载入口，保留 UTF-8 CSV。模板使用中文表头、文本工号/证件号、类别下拉、独立说明页，真正导入页没有示例人员。支持按本校真实部门名称匹配；旧部门编号接口保留，但同样核查学校归属。缺部门、同名歧义、他校部门、重复工号在预览阶段拦截。

人员类别、聘用关系、入职日期需要真实填写，不会在导入预览中把空白悄悄补成“教师/在编/今天入职”。工号留空仍沿用既有编号规则；其他个人资料可后补。导入人员不等于开通账号，不自动转换已有名册口径。

上传 → 校验 → 明确确认 → 逐人原子写入 → 结果回读。错误清单为可用 Excel 打开的 UTF-8 BOM CSV，含原文件行号、字段、原因，不回显整行身份证或其他敏感资料；下载受同校权限和审计保护。支持5000行/5MB限额；拒绝公式、宏、外链、异常压缩和数字形式的长证件号。

### 提交中断可以找回结果，不能盲目重来

提交增加每行事务内的任务所有权凭据、任务锁与行锁。进程失联接管后，旧提交者不能继续写。正式人事主档回读后才保存成功行凭据；四表中一处失败整人回滚，错误行单独记账。

提交响应丢失时，页面不显示成功，也不放开“重新上传”；保留当前任务，先查询服务器。服务器确认旧提交失联超时后，才显示“继续未完成行”；成功行不重复写。状态查询展示已提交数量与当前可回读数量；旧任务没有新凭据时明确不冒充已回读。浏览器完整刷新后的状态恢复逻辑已写入，但本环境未完成实际导航/刷新验收。

### 兼容旧账号，不盲目合并

新库没有 `auth_user` 表时，旧账号迁移直接明确结束，不因查找不存在的旧表导致失败。旧库迁移先核对编号、登录名、安全状态和权限引用；冲突整批拒绝，不因同名跳过、不重置现有密码、不恢复旧高权限。账号迁移仍是部署管理员显式操作，不是公开接口。

## 2. 本轮证据与边界

- `python -m pytest tests -q`：255 项通过，另有25个子测试通过；其中50项是本轮新增的可执行纯规则/CSV/XLSX解析/账号冲突规则测试。其余不少为源码契约测试，不应称为255条真实业务流程。
- 新增 Chromium：16 项交互检查通过，使用真实模板和JavaScript、模拟接口响应。实际操作了模板下载、文件选择、预览、取消提交、断线提示、查询结果、失联接管按钮、取消接管、错误清单下载。页面错误0。
- 既有系统管理 Chromium 回归脚本通过。截图和日志均在 `tests/artifacts/minimal-first-use/`；旧报告保留只供追溯，不作为本轮新证据。
- 浏览器 HTTP 导航被环境策略阻断，已记录原始错误。可执行检查使用真实 Chromium DOM 与 `page.set_content`，不是已启动的 Django 网页。没有改动浏览器安全策略；完整页面刷新及真实存储持久化未验收。
- `python manage.py check` 实际失败：本环境缺少 Django；也未具备可用 MySQL 服务和完整运行依赖。本轮没有执行新迁移、没有在真实 MySQL 开学校或插教师、更没有提交合同/考核/工资。
- 新增13个 Django/MySQL 测试方法已编写，未执行，不能记为通过；覆盖最小开户、回读、不覆盖后补资料、失败回滚、账号兼容入口、真实四表导入、跨校部门、失联任务恢复等。真正并发开户/并发接管压测还需独立验收。

机器可读状态：`FIRST_USE_DELIVERY.json`。完整改动清单：`FIRST_USE_CHANGED_FILES.tsv`；本轮文件哈希清单：`FIRST_USE_FILES.sha256`。框架设计参考 Django 5.2 官方模型字段、事务与 select_for_update 文档；设计依据不等于本机运行证据。

## 3. 部署资产与新增数据库迁移

本包保留完整业务源码与部署结构，不包含运行依赖和字体二进制。已有工程升级请保留原静态字体资产；全新解压时，部署工具可运行 `python scripts/check_first_use_assets.py --baseline-zip /path/to/用户持有的原始基线.zip`，按固定SHA256从用户自己的原包补齐。仅核对可运行 `python scripts/check_first_use_assets.py`；缺项必须先补齐，不能直接忽略字体/图标/工资打印资源报错。本轮没有复制系统字体或附送字体文件。


- `base.0014_minimal_school_bootstrap`
- `employee.0007_optional_employee_phone`
- `hr_staff.0023_import_row_authority_receipt`

尚未在本环境执行。新候选源码不得直接配未迁移的旧库运行。已有环境先完整备份数据库与文件；不要手工删列、重建库、清空人事记录或回退已存在资料。回滚时停止写入，按原版工程和迁移前数据库/文件备份配套恢复；不要仅替换旧代码而忽略新凭据与迁移状态。

## 4. 唯一下一步：独立真实 MySQL 验收

将此完整工程交给实施工具或部署管理员，使用独立测试实例安装原项目依赖与配置；不要先覆盖现有服务器。已有生产部署脚本与 compose 原样保留。

准备就绪后显式确认测试库，再执行：

```bash
export HR_ISOLATED_MYSQL_ACCEPTANCE=YES_I_CONFIRM_TEST_DATABASE
bash scripts/accept_minimal_first_use_mysql.sh
```

脚本要求MySQL，并检查目标测试库为 `test_` 前缀、不同于业务库；Django测试会创建/销毁该测试库。**仍必须使用独立实例和专用验收账号，不能因为名字以 test_ 开头就认定安全。** 不自动创建客户，不自动部署生产。

先补本轮的迁移一致性、真实开户和导入、租户与角色、事务回滚、并发/失联恢复、完整浏览器登录刷新验收，再继续合同、考核、工资和备份恢复主线。任一强制项失败都保留阻断，不用接口模拟或SQLite结果替代。
