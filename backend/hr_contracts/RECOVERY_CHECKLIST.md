# HR07 恢复清单

- [ ] 从 Git 历史、设计文档和现存服务三方确认 HR07 权威模型清单；
- [ ] 确认是否存在丢失的历史 migration，禁止直接生成新 `0001` 覆盖旧库；
- [ ] 建立 `apps.py` 与稳定 app label 前，先证明历史数据库表映射；
- [ ] 恢复模型后跑 MySQL `migrate`、`makemigrations --check`；
- [ ] 恢复合同签订→生效→续签/变更→解除主链；
- [ ] 补 tenant 越权、幂等、并发、有效期、不可变性测试；
- [ ] 验证 HR05 入职与 HR06 异动只通过正式边界交接；
- [ ] 页面逐个对应真实 API，删除无后端能力的假按钮/假状态；
- [ ] 全部通过后才把 `SAFE_TO_REGISTER` 改为 `True`。
