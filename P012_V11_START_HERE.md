# P0/P1/P2 连续基线与验收底座 V11

日期：2026-09-19。唯一输入基线：V10 完整工程；V9→V10 补丁在本轮另行实际复放核对。不是新建或替换系统。

**实际状态：P0文件证据通过；P1原校本配置已重跑受控测试；P2隔离与只读门禁加强，但真实MySQL、完整依赖、生产登录、原生导航、私有文件仍未验收。** 本包不是P2已完成证明，不用于直接覆盖生产。

本轮只改验收覆盖层、验收执行器、只读预检和测试，不改HR05配置业务、数据库迁移、工资、API和页面。V10的权限迁移hr_onboarding.0018以及V8 payroll.0014和所有既有迁移必须保留。

第一入口：`python scripts/run_hr_p012_gate.py`。默认只诊断。需执行时，阅读`docs/p012_v11/OPERATIONS.md`，仅在新隔离本地Docker环境使用`--execute`；不接管旧项目、不读取部署.env、不发布宿主端口。

本轮实际HR05集合346通过、9项MySQL专用测试跳过；原V10在同环境重新收集并执行332项通过，增加14项预检测试。历史报告的343项与本轮原包收集数不同，原因未确定，不沿用历史数字。本轮31项隔离工具单元测试及6项原工具安全测试通过；它们不等于真实Compose/MySQL执行。

P3–P7不在本轮施工范围。校方制度/真实样本/身份系统/邮件服务没有虚构。详细边界、阻断与日志见外部Evidence包和`docs/p012_v11/ACCEPTANCE.md`。
