# 跃科高校人事系统：单校独立部署版系统管理专业化接续入口

日期：2026-09-17  
基线：`Yueke_University_HR_SystemAdmin_Usability_Closure_20260917.zip`  
定位：**单所学校独立部署（B/S，可部署在学校单机服务器/校内服务器），暂不启用平台运营端。**

## 本轮冻结范围

- HR01～HR18 业务模块不扩功能；
- 保留 R5 业务易用性；
- 只收口学校系统管理；
- 一所学校只有一个学校主体；二级学院、职能部门、系部进入组织树；
- 平台运营、套餐、跨学校管理、平台主管临时进入学校接口在单校版默认不开放。

## 系统管理员的唯一主线

```text
学校主体
→ 组织/学院/部门
→ 岗位目录
→ 管理员账号
→ 角色与最小权限
→ 基础参数/字典
→ 邮件与通知
→ 安全审计
→ 接口（如学校实际需要）
→ 生产备份
→ 隔离恢复演练
→ 上线验收
```

## 新增专业化能力

1. `HR_INSTALLATION_MODE=standalone_school` 明确部署形态。
2. `PLATFORM_OPERATIONS_ENABLED=False`，单校版不发布平台主管 elevation API。
3. 普通界面只允许创建一个学校主体；学院/部门不能再误建成第二所学校。
4. 部门表单在唯一学校下自动绑定本校，不再重复选择学校。
5. 系统管理首页按任务组织：学校信息、账号、角色权限、组织、岗位、参数、审计、邮件、接口、运行备份。
6. 新增专业运行检查：学校主体、组织岗位、角色、MySQL、安全基线、MFA、邮件、备份。
7. 检查结果只暴露状态，不暴露密码、Token 或加密密钥。
8. 新增服务器命令：`python manage.py check_school_system`；有阻断项时 fail-closed。
9. 单校版本地超级管理员只允许进入唯一学校；多学校记录时拒绝隐式写入。
10. 数据库恢复仍只允许服务器受控流程，不增加危险的网页“一键恢复”。

## 当前验证

- `tests/`：205 passed + 25 subtests；
- 系统管理旧门：31/31 PASS；
- HR 易用性门：102/102 PASS；
- 系统管理真实 Chromium 静态交互：PASS，pageerror=0；
- 14 段 HR 生命周期 Chromium：PASS；
- Python AST：3168/3168 PASS；
- JavaScript `node --check`：256/256 PASS。

## 仍需真实环境验证

当前云沙箱没有完整 Django/MySQL 运行环境，因此以下不能冒充已经完成：

- `manage.py check` 的真实 Django 启动；
- 单校 MySQL migration；
- 管理员真实登录与权限即时生效；
- 生产备份 → 空库恢复；
- 学校 SMTP/SSO/数据接口联调；
- 学校服务器运行与并发证据。

这些属于部署验收，不再属于继续扩源码功能。
