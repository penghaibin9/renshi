name: Archived / Patch Base MySQL Fresh Schema

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical repair is permanently archived
        run: >-
          echo "The MySQL fresh-schema migration repair is already in repository
          history. This workflow is retained as a read-only archive and performs
          no checkout, commit, or push."
