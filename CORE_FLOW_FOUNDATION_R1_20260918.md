# 跃科高校人事系统：核心业务主链底座 R1

## 本轮定位

不增加菜单，不进入合同、考核、工资，不部署生产。继续收口高校人事系统最关键的“组织 + 人员可信底座”。

## 核心业务方向

系统最终要保证：一个教职工从进入学校到离开学校，只有一套可追溯的人事事实；招聘、入职、异动、合同、资格、培训、考勤、考核、职称、岗位、薪酬、退休和本人服务都引用这套事实，而不是各模块各存一份互相打架的数据。

## 本轮实际修改

1. 首次使用页不再以“存在 HrStaffMaster”作为“第一位真实教职工已完成”的依据。
2. 必须回读到同校 Person → StaffMaster → ACTIVE Employment → ACTIVE Assignment 四层链路才算人员底座完成。
3. 增加真实 MySQL 验收用例：完整四层应通过；删除任职或只造半条 StaffMaster 必须保持未完成。
4. 增加非数据库源码合同测试，防止后续改代码时退回“只看一张人员表”的假完成逻辑。

## 已执行

- `python -m pytest tests/minimal_first_use -q`：53 passed。
- `python -m pytest tests -q`：258 passed, 25 subtests passed。

这些属于纯规则/源码合同回归，不等于真实 MySQL 验收。

## 仍然阻断

上一轮真实 MySQL 验收环境依赖仍未补齐，因此最小开户 → 登录 → HR02 组织 → HR03 Excel → 四层正式回读 → 中断恢复仍需在独立 MySQL 实例实际跑通。通过前不进入 HR07 合同、HR12 考核、HR15 薪酬。

## 下一施工点

在真实 MySQL 门禁可运行前，代码侧下一高价值点仍只在 HR02/HR03：把“组织/岗位”和“人员任职”之间的 Authority 对账做硬，避免后续合同、考核、岗位聘任和工资引用到失效/错校组织事实。
