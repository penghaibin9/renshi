# V15 核心业务流程回放与收口

日期：2026-09-19。连续基线：V14（R11 通用证据能力融合）。

本版不增加新的一级模块，目标是把 HR01–HR18 按“办完一件事”整理成 14 条核心业务链，并在当前隔离环境中逐链回放可执行的服务/数据库契约。发现的产品问题直接修复；必须 MySQL、完整生产依赖、真实学校账号或外部系统才能证明的项目保持为环境门，不以 SQLite、测试夹具或静态检查冒充生产通过。

本轮三项产品修复：
1. HR06 借调/挂职保留原岗时，临时任职不再默认 1.0 FTE 导致真正生效阶段超过 HR03 1.50 总 FTE 上限；改为按计划生效日核验原岗和现有任职，计算剩余额度并冻结到异动提案，仍由 HR03 正式校验兜底。
2. HR10 培训申请创建时，班次与项目、人员身份在同一事务中加锁核对，减少并发下项目/班次关系变化造成错误申请的窗口。
3. HR09 消费 HR10 发展事实时，如果人员既没有旧身份桥接、也没有任何 UUID 正式事实链，不再返回“OK 空数据”，而是明确来源身份映射不可用，避免资质审核把未知误判为没有事实。

本轮没有新增数据库迁移。V14 的 `hr_assessment.0028_archive_evidence_access` 及所有历史迁移保持。

详细流程、测试证据和边界见 `docs/flow_validation_v15/CORE_FLOWS.md` 与 `FLOW_RESULTS.json`。
