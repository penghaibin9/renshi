# V13｜本批代码收尾入口

连续基线：V12，未另建系统。请先读 `docs/closeout_v13/CLOSEOUT.md` 与 `REMAINING.json`。
本包是代码修复和受控回归的收尾交付，不是生产上线、真实学校发薪或客户验收证明。

## 当前应使用什么

以本 V13 完整源码为新基线；已经有并行修改时只合并相对 V12 的补丁。原 V12 客户材料仍保留并适用；这份入口补充新的回读、恢复和证据兼容要求。

## 不要做什么

不要清库、重建库、批量重算工资、覆盖并行代码、解除 MySQL 门禁或用 SQLite 替代生产验收。
不要将共库多校的整库/媒体交接包外发给单个学校。代码 ZIP 不包含本轮运行数据库、安装依赖、私钥或系统字体。

## 本轮变化

导入提交结果和迁入台账统一严格回读；快速查询只接收最新请求；两套恢复入口共享路径和压缩包保护；内容指纹升级 v2，证据文件私有且不覆盖旧文件。
没有新迁移，保留全部历史迁移与原薪酬代码；前后端必须一起更新，不能只更新 CSS/JS。

## 发布顺序

先在独立预发布合并代码并保留备份 → 原锁依赖与全部历史迁移 → 原静态资源更新 → 真实账号、迁入、材料/责任任务回放 → MySQL 独立恢复 → 校方验收。
P2 当前仍阻断。具体未完成事项只以 `REMAINING.json` 和本轮证据为准，不沿用旧版摘要的完成数字。
